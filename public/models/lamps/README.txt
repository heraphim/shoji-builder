Saved lamps live here — *.lamp.json, exported from the Lamp file panel.
The folder is listed automatically (see the library-index plugin in vite.config.ts);
drop a file in and it shows up next time the Open lamp picker is opened.
